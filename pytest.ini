[pytest]
# Run from the repo root; make the slackarchive package importable without installing
# (mirrors how the scripts set PYTHONPATH to the repo root).
pythonpath = .
testpaths = tests
addopts = -q
